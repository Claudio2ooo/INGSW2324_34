tutti gli attributi basati su soldi:
    bigint che memorizza in centesimi(strategia più usata)
timer dell'asta:
    integer che memorizza i minuti del timer(forse da rivedere)
immagine asta:
    stringa che memorizza l'url (in aws o locale)

vincoli su asta da definire

backup del db:
    sudo docker exec -t <nome_container_postgres> pg_dumpall -c -U postgres > dump_`date +%d-%m-%Y"_"%H_%M_%S`.sql

restore:
    cat <dump>.sql | docker exec -i <container_postgres> psql -U postgres

